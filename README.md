# Blueberries - Маркетплейс

Итоговый проект по дисциплине HTML/CSS/JavaScript. Полнофункциональный маркетплейс с React фронтендом и Node.js бэкендом.

## 🚀 Технологии

### Frontend
- **React 18** с TypeScript
- **Vite** - сборщик и dev-сервер
- **React Router** - маршрутизация
- **Chart.js** - графики и визуализация данных
- **Leaflet** - карты и геолокация
- **CSS3** - современные стили с градиентами и анимациями

### Backend
- **Node.js** с Express
- **TypeScript** - типизация
- **SQLite** - база данных
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей

## 📁 Структура проекта

```
htmlcssitogoviy/
├── frontend/          # React приложение
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── context/      # React Context (Auth)
│   │   ├── services/     # API сервисы
│   │   ├── styles/       # CSS стили
│   │   └── utils/        # Утилиты
│   └── package.json
├── backend/          # Node.js API
│   ├── src/
│   │   ├── controllers/  # Контроллеры
│   │   ├── routes/       # Маршруты
│   │   ├── middleware/   # Middleware
│   │   ├── database/     # БД и миграции
│   │   └── utils/        # Утилиты
│   └── package.json
└── assets/           # Статические ресурсы
```

## 🛠️ Установка и запуск

### Предварительные требования
- Node.js 18+ 
- npm или yarn

### Backend

```bash
cd backend
npm install
npm run dev
```

Backend будет доступен на `http://localhost:3000`

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

### Сборка для production

**Backend:**
```bash
cd backend
npm run build
npm start
```

**Frontend:**
```bash
cd frontend
npm run build
```

Собранные файлы будут в `frontend/dist/`

## 📡 API Endpoints

### Авторизация
- `GET /api/auth` - информация об API
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход

### Отзывы/Посты
- `GET /api/posts` - получить все отзывы (публичный)
- `GET /api/posts/:id` - получить отзыв по ID (публичный)
- `POST /api/posts` - создать отзыв (требует авторизации)
- `PUT /api/posts/:id` - обновить отзыв (только автор)
- `DELETE /api/posts/:id` - удалить отзыв (только автор)

## 🔐 Аутентификация

API использует JWT токены. После успешной регистрации/входа токен сохраняется в localStorage и автоматически добавляется в заголовок `Authorization: Bearer <token>` для защищенных запросов.

## 🗄️ База данных

Используется SQLite с автоматическими миграциями. База данных создается автоматически при первом запуске.

### Таблицы:
- `users` - пользователи
- `posts` - отзывы/посты
- `migrations` - история миграций

## 🎨 Особенности

- ✅ Адаптивный дизайн (Mobile First)
- ✅ Градиентный фон с анимацией
- ✅ Glassmorphism эффекты
- ✅ Плавные анимации и переходы
- ✅ Интерактивная карта с геолокацией
- ✅ Дашборд с графиками и таблицами
- ✅ Карусель промо-баннеров
- ✅ Модальное окно авторизации
- ✅ Защищенные маршруты

## 📝 Переменные окружения

### Backend (.env)
```env
PORT=3000
NODE_ENV=development
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d
```

### Frontend (.env)
```env
VITE_API_URL=http://localhost:3000/api
```

## 🧪 Тестирование

```bash
# Backend
cd backend
npm test

# Frontend
cd frontend
npm test
```

## 📦 Развертывание

1. Установите зависимости для backend и frontend
2. Настройте переменные окружения
3. Соберите frontend: `npm run build`
4. Соберите backend: `npm run build`
5. Запустите backend: `npm start`
6. Разместите `frontend/dist/` на статическом хостинге или настройте Express для раздачи статики

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Итоговый проект по дисциплине HTML/CSS/JavaScript

